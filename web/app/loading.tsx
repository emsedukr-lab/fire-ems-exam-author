export default function Loading() {
  return (
    <main className="shell">
      <div className="shell-grid">
        <section className="hero-card skeleton-block large" />
        <section className="panel skeleton-block" />
        <section className="panel skeleton-block" />
      </div>
    </main>
  );
}
